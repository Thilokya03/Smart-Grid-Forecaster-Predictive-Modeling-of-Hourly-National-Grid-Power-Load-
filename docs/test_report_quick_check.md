# Test Report Quick Check

Generated for the current submission evidence on 2026-09-20.

## Required Submission Sections

| Required section | Current status | Quick note |
| --- | --- | --- |
| SW Testing | Pass locally and hosted smoke passes | Pytest, JavaScript, dashboard browser checks, failure/recovery, E2E checks, Supabase smoke, Render smoke, local performance benchmarks, and coverage evidence are recorded. |
| Evaluation of DS Parts | Provisional | June holdout artifacts and Prophet CV were regenerated from corrected data, but XGBoost, SARIMAX, and DNN/LSTM CV artifacts still need corrected-data regeneration. |
| Error Analysis of DS Parts | Complete for current artifacts; provisional for final sign-off | Weighted Ensemble June residual analysis is generated by hour, day type, holiday, season, demand quartile, temperature band, residual distribution, residual autocorrelation, and worst errors. |

## Passed

| Area | Result | Evidence |
| --- | --- | --- |
| Python automated tests | 78 passed, 0 failed in the generated report | `results/test-reports/pytest-report.xml` |
| JavaScript logic tests | 8 passed, 0 failed | `results/test-reports/javascript-output.txt` |
| Public dashboard smoke | Pass | `results/test-reports/public-dashboard-output.txt` |
| Admin model comparison smoke | Pass | `results/test-reports/admin-output.txt` |
| Super-admin health smoke | Pass | `results/test-reports/super-admin-output.txt` |
| Local monitored pipeline refresh | Pass, 9 steps | `artifacts/pipeline_status/run.json` |
| Supabase PostgreSQL smoke | Pass: required tables are non-empty and 24 forecast rows are readable | `results/test-reports/external-smoke.json` |
| Deployed Render dashboard smoke | Pass: 24h and 168h forecasts, admin comparison, and super-admin health endpoint are reachable | `results/test-reports/external-smoke.json` |
| Local performance benchmarks | Pass: all measured in-process payloads completed under 12 ms max in the latest run | `results/test-reports/performance-benchmarks.json` |
| Coverage measurement | 23.00% statement coverage across measured Python packages | `results/test-reports/coverage.xml` and `results/test-reports/coverage-output.txt` |
| Data integrity | Pass: no duplicate timestamps, missing hours, missing demand, or missing temperature in master data | `docs/test_report_submission.md` |
| Failure and recovery automated tests | 9 pass, 1 not applicable, 1 manual pending | `docs/test_report_submission.md` |
| End-to-end and integration tests | 5 pass | `docs/test_report_submission.md` |
| DS metrics and error analysis generation | Generated | `results/test-reports/ds/` |

## Failed Or Warning

| Area | Current result | What it means |
| --- | --- | --- |
| Render pipeline health | Warning with 2 alerts in the generated evidence | Public forecast endpoints passed, but the health alerts should be reviewed or explained before final submission. |

## Still Needed Before Final Submission

| Item | Type | Why it is still needed |
| --- | --- | --- |
| Render restart recovery check | Manual hosted check | Confirms the deployed service keeps working after a real host/container restart. |
| Retrain remaining CV artifacts on corrected canonical data | DS completion | Remaining audit mismatches are 367: XGBoost CV 131, SARIMAX CV 131, and DNN/LSTM CV 105. |
| Regenerate final DS report after remaining CV refresh | DS completion | Produces final metrics from fully corrected-data model artifacts. |
| Review Render health alerts | Operational validation | Either fix the warnings or document why they are acceptable. |
| Attach evidence bundle | Submission packaging | Include report, CSV evidence, XML/text outputs, and screenshots if required by the assessor. |

## Recommended Manual Testing

| Manual test | Status | Steps to record |
| --- | --- | --- |
| Public dashboard visual check | To do | Open the deployed URL, confirm 24h/168h forecasts render, chart/table values look populated, and downloads work. |
| Admin comparison UI check | To do | Open admin view with token, confirm model rows and metrics match the generated report. |
| Super-admin health UI check | To do | Open health view with token, screenshot status, alerts, and forecast coverage. |
| Render restart recovery | To do | Restart the Render service, wait until healthy, rerun `python tools/run_external_smoke.py --strict`, and capture the result. |
| Supabase table spot check | Done by external smoke | Required tables are non-empty and 24 `fast_xgboost` forecast rows are readable. |
| Negative access check | To do | Try admin/super-admin routes with no token or a wrong token and confirm access is denied. |

## Safe Local Environment Setup

It is okay to keep testing secrets in a local `.env` file. `.gitignore` already excludes `.env`, `.env.*`, and `results/`, so these values should not be committed.

Expected local keys:

```env
DATABASE_URL=
PUBLIC_DASHBOARD_URL=
PREVIEW_ADMIN_TOKEN=
PREVIEW_SUPER_TOKEN=
```

Run the final external check with:

```powershell
.\.venv\Scripts\python.exe tools/run_external_smoke.py --strict
```
